
bin_PROGRAMS=spawn-fcgi spawn-manager

INCLUDE_FILES = spawn-common.h

include_HEADERS = $(INCLUDE_FILES)

spawn_fcgi_SOURCES=spawn-fcgi.c
spawn_manager_SOURCES=spawn-manager.c

EXTRA_DIST=CMakeLists.txt config.h.cmake
